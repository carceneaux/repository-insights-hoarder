{
  "dependencies": {
    "@actions/core": "^2.0.1",
    "@actions/github": "^6.0.1",
    "path": "^0.12.7"
  },
  "scripts": {
    "test": "jest",
    "build": "ncc build index.js --license licenses.txt -m"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "jest": "^30.2.0"
  }
}
